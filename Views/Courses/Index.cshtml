@{
  Layout = "_Layout";
}

<header>
  <p><strong>@Html.ActionLink("ADD NEW COURSE", "Create")</strong></p>
</header>

@using UniversityRegistrar.Models;

<h1 id="get-low">Courses<h1>
<hr>
@if (@Model.Count == 0)
{
  <p>No course have been added</p>
}

@foreach (Course course in Model)
{
  <h4>@Html.ActionLink($"{course.Name}", "Details", new { id = course.CourseId })</h4>
  <br>
}